services:
  mysql:
    image: mysql:8.0
    container_name: ftms-mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
    ports:
      - "3306:3306"
    volumes:
      - ftms-mysql-data:/var/lib/mysql

volumes:
  ftms-mysql-data:
